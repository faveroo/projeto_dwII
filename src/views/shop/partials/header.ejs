<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema Premium</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="container">
        <div class="header">
            <div>
                <h1>Dashboard</h1>
                <% if (user) { %>
                    <p class="welcome-text">
                        Olá, <strong>
                            <%= user.name %>
                        </strong>
                        <span class="role-badge role-<%= user.role %>">
                            <%= user.role %>
                        </span>
                    </p>
                    <% } %>
            </div>
            <% if (user) { %>
                <a href="/auth/logout" class="logout-link">
                    <span>Sair</span>
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none"
                        stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path>
                        <polyline points="16 17 21 12 16 7"></polyline>
                        <line x1="21" y1="12" x2="9" y2="12"></line>
                    </svg>
                </a>
                <% } %>
        </div>

        <% if (user) { %>
            <nav class="main-nav" style="margin-bottom: 2rem;">
                <a href="/shop" class="nav-link">Início</a>

                <% if (user.role==='admin' ) { %>
                    <a href="/product/create-product" class="nav-link">Criar Produto</a>
                    <a href="/category/create-category" class="nav-link">Criar Categoria</a>
                    <a href="/coupon/create-coupon" class="nav-link">Criar Cupom</a>
                    <% } %>

                        <% if (user.role==='user' ) { %>
                            <a href="/order/create-order" class="nav-link">Fazer Pedido</a>
                            <% } %>
            </nav>
            <% } %>